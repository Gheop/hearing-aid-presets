# Drop-in for bluetooth.service: keep the vcp plugin out of the way so the
# extension can drive per-ear volume. The bluetoothd path below is Fedora's;
# take yours from: systemctl show -p ExecStart bluetooth
[Service]
ExecStart=
ExecStart=/usr/libexec/bluetooth/bluetoothd --noplugin=vcp
